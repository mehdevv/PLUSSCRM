VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_publishable_key

# Server-side only (seed scripts) — never prefix with VITE_
# Use Legacy "service_role" OR new "sb_secret_..." from API Keys tab
SUPABASE_SERVICE_ROLE_KEY=your_service_role_or_secret_key

# Groq API key for the admin AI assistant (server-side only — never prefix with VITE_)
GROQ_API_KEY=your_groq_api_key
